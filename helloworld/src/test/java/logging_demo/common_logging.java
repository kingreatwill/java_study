package logging_demo;

public class common_logging {
    // commons-logging 自带LogFactory
    // commons-logging与SLF4J一样
    // 按照一套统一的API来进行日志编程，实际的日志框架来实现这套API，这样的话，即使更换日志框架，也可以做到无缝切换。这就是commons-logging与slf4j的初衷。
}
